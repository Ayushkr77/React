export default function Footer() {
    return (
        <footer>
            <p>&copy; {new Date().getFullYear()} Your website name</p>  {/* JS code is written inside the curly brackets if we are writing JS code inside return block */}
        </footer>
    )
}